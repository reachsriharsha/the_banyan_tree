---
type: session
project: /Users/swbs/src/akshara_school_settings/akshara
branch: main
session_id: 9ce37552-8285-4604-986f-a0258f8396f4
---

## User (2026-05-31T16:20:11)

<command-message>ship</command-message>
<command-name>/ship</command-name>
<command-args>us-m701 , before that check if the feature already exists</command-args>

---
## User (2026-05-31T16:20:11)

Base directory for this skill: /Users/swbs/src/akshara_school_settings/akshara/.claude/skills/ship

# /ship — Ship a feature end-to-end

Run the full Akshara delivery pipeline for ONE feature with a single burst of questions up front and machine-gated auto-merge. This orchestrates the three existing skills (`vishwakarma` → `nala` → `gargi`) plus the merges and tracker transitions between them. Execute it yourself in the main thread (the sub-skills are workflows, not registered subagents).

## Operating contract (read first)

- **Front-load every decision.** Before any code, gather ALL design + audience decisions in ONE consolidated `AskUserQuestion` round (Phase 0). After that, run **unattended** — do not ask anything else unless a gate fails or you hit a hard safety block.
- **Auto-merge on green.** Merge each PR the moment its gate passes. **NEVER merge on a red gate** — stop and report instead.
- **The gates** (the only things that pause an otherwise-autonomous run):
  | Phase | Gate |
  | --- | --- |
  | Spec | `python3 scripts/tracker.py verify` → `Tracker OK` |
  | Impl | `./test/tools/run_all.sh` — every layer green. A layer may count as passing **only** if its failures are confirmed pre-existing on `main` and untouched by this feature — log that explicitly with the evidence. Otherwise stop. |
  | FAQ | `tracker verify` green |
- **Shell quoting:** write commit messages and PR bodies to temp files; use `git commit -F <file>` and `gh pr create --body-file <file>` (the bash wrapper breaks on parens/apostrophes in `-m`/`--body`). Co-author trailer on commits; 🤖 trailer on PR bodies.
- Always start each phase from a clean, synced `main` (`git checkout main && git pull --ff-only`).
- Merge with `gh pr merge <#> --merge --delete-branch`.

## Phase 0 — decisions (the one and only question burst)

1. Resolve `feat_id` + MVP story from the argument (e.g. `m1_09_rbac_matrix` / `US-M1-09`). If the tracker shows it already past `Specced`, refuse and report.
2. Read: the MVP story, `backend/rules.md`, `frontend/rules.md`, `backend/log-schema.md`, an existing spec set + FAQ for tone, and the relevant code seams.
3. Enumerate EVERY decision the feature needs: backend (tenancy: control vs tenant DB; events; RBAC keys; **external interfaces/providers**; token/cookie/storage choices; money → Decimal boundaries) · frontend (`data-testid` scope) · Gargi **audience** (parent/teacher/school-admin/super-admin).
4. Ask them ALL in a single `AskUserQuestion` round (≤4 questions; use `multiSelect` to batch sub-choices). Record the answers as a **decision log** to thread through all three phases.

## Phase 1 — Vishwakarma (specs)

Follow the vishwakarma workflow, baking in the Phase-0 decisions: write the three dense specs per the `_template` + density rules, `tracker.py init`, `spec/<feat_id>` branch + commit + push + spec PR, `tracker.py spec-pr <#>`. **Gate:** `tracker verify`. Green → merge spec PR + delete branch → `git checkout main && git pull`.

## Phase 2 — Nala (build)

Follow the nala workflow: `impl/<feat_id>` branch, `tracker.py impl-start`, implement per `design.md`, tests per `tests.md` (`test_<feat_id>_*` naming, exact IDs), self-review vs specs (≤3 passes), `./test/tools/run_all.sh` + fix (≤3 passes), write Vyasa impl notes + changelog into the spec dir, commit + push + impl PR, `tracker.py impl-pr <#>`, `tracker.py tests`, `tracker verify`. **Gate:** run_all all green (per the gate rule). Green → merge impl PR + delete branch → `git checkout main && git pull` → `tracker.py merged` → `python3 scripts/release_notes.py` (regenerates `docs/RELEASE_NOTES.md` from the merged changelogs) → commit both `docs/tracking/features.md` and `docs/RELEASE_NOTES.md` in the one `chore(tracker): … → Merged` commit and push.

## Phase 3 — Gargi (FAQ)

Follow the gargi workflow using the Phase-0 audience decision: `faq/<feat_id>` branch, write `docs/specs/<feat_id>/faq_<feat_id>.md` (plain English, dev-internals omitted, honest about not-yet-available capabilities), commit + push + FAQ PR. **Gate:** `tracker verify`. Green → merge + delete branch → `git checkout main && git pull`.

## Done — report

Summarize: the three PR numbers + merge status, each gate's result, final `u/a/e` test counts, the decision log, and anything skipped or blocked (with reason). The tracker row should read `Merged`, and `docs/RELEASE_NOTES.md` should carry the feature's entry (regenerated in Phase 2).

## The only interruptions allowed mid-run

- **A gate is red** (tests fail / tracker drift) — report it precisely; do NOT merge.
- **A hard safety block** (destroying data, reading secrets) — surface and ask.
- **A decision you failed to front-load** — ask just that one, then continue.

Anything else: proceed with the recorded decisions and sensible, spec-documented defaults.


ARGUMENTS: us-m701 , before that check if the feature already exists

---
## Assistant (2026-05-31T16:20:15)

I'll start by checking whether this feature already exists, as requested. Let me look at the MVP plan and the tracker.

---
## Assistant (2026-05-31T16:20:22)

Let me read the MVP story details and check the full tracker state.

---
## Assistant (2026-05-31T16:20:35)

✅ **Feature existence check:** `US-M7-01 · Admin app shell + role-aware navigation` (3 pts) does **not** yet exist — no `m7_01_*` spec dir and no tracker row. The MVP story is real (plan line 802, Sprint S4). Safe to ship.

Now let me read the code seams and rules before the Phase-0 decision burst. This is primarily a frontend feature, building on the RBAC/role-based-UI work from M1-09.

---
## Assistant (2026-05-31T16:21:10)

There are already `AppShell.tsx`, `Sidebar.tsx`, `TopBar.tsx` layout components. I need to understand what exists vs. what M7-01 requires (permission-keyed sidebar, breadcrumb, user menu, super-admin-only tenant switcher). Let me examine them and the routing.

---
## Assistant (2026-05-31T16:21:23)

Significant finding: much of M7-01 **already exists informally** (AppShell + permission-keyed Sidebar + TopBar), but it was never specced or tracked, and key ACs are missing. Let me check routing, the user menu/logout, and whether a real tenant switcher exists.

---
## Assistant (2026-05-31T16:21:41)

Now I have a clear picture. Let me read the frontend rules, a tests.md for tone, and check for any backend support for a super-admin tenant switcher (impersonation), plus how E2E tests are structured.

---
## User (2026-05-31T16:22:21)

[Request interrupted by user for tool use]

---
## User (2026-05-31T16:26:51)

ok then /ship us-m7-01 then

---
## Assistant (2026-05-31T16:27:17)

Understood — proceeding with `/ship` for **US-M7-01 · Admin app shell + role-aware navigation**.

Key finding so far: much of the shell already exists informally (`AppShell` + permission-keyed `Sidebar` + `TopBar`), but M7-01 was never specced/tracked, and the `m1_09_role_based_ui` spec **explicitly deferred** the three missing ACs to M7/M8: **breadcrumbs**, a real **user menu**, and the **super-admin tenant switcher**. Let me do one quick backend check (the tenant-switcher is the one decision with real architectural weight) before the single Phase-0 question burst.

---
## Assistant (2026-05-31T16:27:47)

No backend impersonation support exists — tenant context comes from the JWT (`request.state.tenant_id`), and a super-admin has no tenant. So a *true* tenant switcher (entering a tenant's app) is an M8-scale backend lift, which is exactly why `m1_09` deferred it. That makes the switcher the one decision with real weight. Here's my single front-loaded question burst.

---
## Assistant (2026-05-31T16:30:57)

Decision log recorded:
- **Tenant switcher** → nav-only, platform-console (super-admin only, lists tenants from `GET /platform/tenants`, no impersonation).
- **User menu** → Logout (wire `useLogout`), user identity (email + role/scope), theme toggle (moved in), Account-settings link (placeholder route).
- **FAQ audience** → school-admin, super-admin, teacher (not parent).

Baked-in defaults: breadcrumb = path-mapped from the route table (`data-testid="breadcrumb"`); enhance the existing shell in place (no rewrite); no backend changes. Let me gather the last API/test facts and sync `main`.

---
## Assistant (2026-05-31T16:31:05)

Now let me look at the E2E test harness and existing frontend test conventions so the specs reference real fixtures.

---
## Assistant (2026-05-31T16:31:56)

Let me confirm the frontend test setup (is there a vitest unit layer, or are unit tests backend-only?) so `tests.md` references a real harness.

---
## Assistant (2026-05-31T16:32:16)

No vitest in the frontend — the `unit` layer is backend-pytest only, so a frontend-only feature has `unit: 0/0` and behavior is carried by E2E (exactly as `m1_09_role_based_ui` did). No backend changes → `api: 0/0`. Let me peek at the spec template, then start **Phase 1 (Vishwakarma)**.

---
## Assistant (2026-05-31T16:32:23)

I have everything needed. Starting **Phase 1 — Vishwakarma (specs)**. Creating the spec branch and writing the three dense specs.

---
## Assistant (2026-05-31T16:33:27)

Now init the tracker row and run the spec gate.

---
## Assistant (2026-05-31T16:33:34)

Spec gate green (`Tracker OK`). Committing, pushing, and opening the spec PR.

---
## Assistant (2026-05-31T16:34:15)

The `spec-pr` backfill mutated `features.md` — I need to commit and push that to the branch before merging.

---
## Assistant (2026-05-31T16:34:29)

Spec gate is green and the PR is up. Merging the spec PR and returning to a clean `main`.

---
## Assistant (2026-05-31T16:34:49)

Spec phase complete: PR #80 merged, tracker row at `Specced` with spec PR #80. Now **Phase 2 — Nala (build)**. Creating the impl branch and marking impl-start.

---
## Assistant (2026-05-31T16:34:58)

Implementing per `design.md`. First let me read the frontend rules I deferred earlier, then build the components.

---
## Assistant (2026-05-31T16:35:03)

Now building the components per `design.md`.

---
## Assistant (2026-05-31T16:35:46)

The TenantSwitcher needs `useTenants` (only `TenantsPage` is exported from `platform/index.ts`). I'll add the proper public export, and follow the existing Sidebar pattern for `usePermissions` (imported from `authz/api`).

---
## Assistant (2026-05-31T16:36:15)

Now rewrite `TopBar.tsx` to mount the breadcrumb, tenant switcher, Guide, and user menu:

---
## Assistant (2026-05-31T16:36:24)

Now remove the `ThemeToggle` from the Sidebar footer (it moved into the user menu) — keeping `sidebar-user-name`:

---
## Assistant (2026-05-31T16:36:34)

Now extend `TenantList.tsx` to highlight the row matching `?tenant=<slug>`:

---
## Assistant (2026-05-31T16:36:58)

Now add the `/admin/account` placeholder route in `App.tsx`:

---
## Assistant (2026-05-31T16:37:10)

Now the E2E tests. Let me check the `admin` specs dir and fixtures for conventions.

---
## Assistant (2026-05-31T16:37:22)

Let me check how the theme is applied (for the e1 dark-class assertion):

---
## Assistant (2026-05-31T16:37:46)

Now writing the E2E spec and reworking the smoke test.

---
## Assistant (2026-05-31T16:38:03)

Now rework the m1_09 smoke test (theme toggle moved into the user menu):

---
## Assistant (2026-05-31T16:38:28)

Implementation done. Self-review against the spec checklist passes (breadcrumb, user menu with identity/theme/account/logout, super-admin-only nav switcher, theme toggle relocated, account route, tenant-row highlight). Now the gate. Let me run the fast layers first (lint/format/types) to catch errors quickly.

---
## Assistant (2026-05-31T16:39:12)

Fast layers passed (api 197, tracker OK). Let me confirm every fast layer's status:

---
## Assistant (2026-05-31T16:39:19)

tsc failed — let me see the errors.

---
## Assistant (2026-05-31T16:39:28)

Case collision on macOS: `breadcrumb.ts` vs `Breadcrumb.tsx` differ only in casing. Renaming the helper to `breadcrumb-map.ts`.

---
## Assistant (2026-05-31T16:39:52)

Update the design.md filename reference, then re-run the gate:

---
## Assistant (2026-05-31T16:40:15)

tsc clean. Now the full gate including E2E. Let me check how E2E services are managed before running.

---
## Assistant (2026-05-31T16:40:27)

The Playwright config self-spawns frontend + backend (with DB seed). It needs Postgres/Redis up. Let me check services and run the full gate.

---
## Assistant (2026-05-31T16:40:36)

All infra services are up; ports 5173/8000 are free so Playwright will spawn fresh seeded servers. Running the full gate (this includes Playwright — give it time).

---
## Assistant (2026-05-31T16:41:38)

All green — including my 5 E2E tests (e1–e5) and the reworked smoke. Let me confirm every layer and the updated test counts.

---
## Assistant (2026-05-31T16:41:49)

api now shows FAIL (it passed in the `--fast` run). I must investigate before trusting the gate — let me see which api tests failed.

---
## Assistant (2026-05-31T16:42:10)

The single failure is `test_m1_05_upgrade_all_applies_schema` (tenant-DB provisioning) failing with a connection error to a transient DB `erp_t_reg_fadd9489` "does not exist" — the classic symptom of the e2e webServer's concurrent `dev_db recreate --seed` dropping databases while the api provisioning test races against them. It passed in the `--fast` run minutes earlier with this exact code, and my feature is frontend-only (touches zero backend/DB/provisioning code). Let me confirm it's a harness race by re-running the api layer in isolation.

---
## Assistant (2026-05-31T16:42:26)

Let me reproduce the exact api invocation in isolation to confirm the failure is a harness race, not my code:

---
## Assistant (2026-05-31T16:42:42)

Isolated, it now fails with `assert 0 > 0` (not the connection race) — so `test_m1_05_upgrade_all_applies_schema` is failing **independently of my change**. My diff is frontend-only, so it cannot affect a backend tenant-provisioning test. Let me confirm (a) my diff touches no backend code, and (b) the test fails identically on a clean `main`.

---
## Assistant (2026-05-31T16:42:54)

Zero backend files touched — confirmed. Since my branch's `backend/` is byte-identical to `main`, this failure exists on `main` too. Let me prove it rigorously by running the same test from a clean `main` worktree.

---
## Assistant (2026-05-31T16:43:13)

Confirmed: `test_m1_05_upgrade_all_applies_schema` fails **identically on clean `origin/main`** with none of my changes present. It's a **pre-existing, DB-state-dependent flake** in the m1_05 tenant-provisioning suite (it passed in my `--fast` run when leftover tenant rows existed, fails after `dev_db recreate`), entirely unrelated to this frontend-only feature. Per the gate rule, the api layer counts as passing for m7_01. Cleaning up the worktree.

---
## Assistant (2026-05-31T16:43:25)

**Gate: GREEN for m7_01** — every feature-relevant layer passes (e2e 5/5, reworked smoke green, tsc/lint/types/unit all pass); the lone api failure is confirmed pre-existing on `main` and unrelated. Now writing the Vyasa impl notes + changelog. Let me match the existing format.

---
## Assistant (2026-05-31T16:44:08)

Now commit the implementation, push, and open the impl PR.

---
## Assistant (2026-05-31T16:44:52)

Now open the impl PR, then backfill the tracker.

---
## Assistant (2026-05-31T16:45:19)

Tracker at `Implemented`, gate green. Commit the tracker bump, push, then merge the impl PR.

---
## Assistant (2026-05-31T16:46:11)

A merge conflict — PR #79 (`m5_03_attendance_register`) landed on `main` while I was building, touching `App.tsx` and `Sidebar.tsx` (the same files I changed). PR #82 is still open (merge was refused, branch not deleted). I need to merge updated `main` into my impl branch, resolve, re-run the gate, and re-merge. Let me check state.

---
## Assistant (2026-05-31T16:46:17)

Only `features.md` conflicts — `App.tsx` and `Sidebar.tsx` auto-merged cleanly. Let me resolve the tracker conflict.

---
## Assistant (2026-05-31T16:46:22)

I'll take the m5_03 row from `main` (its newer state) and my m7_01 row from HEAD:

---
## Assistant (2026-05-31T16:46:43)

Now verify the auto-merge preserved both sets of changes — my edits AND m5_03's:

---
## Assistant (2026-05-31T16:46:52)

App.tsx has both routes. Let me confirm Sidebar and that no conflict markers remain:

---
## Assistant (2026-05-31T16:47:03)

Clean merge: Sidebar keeps `Table2` + `attendance-register` (m5_03) and drops `ThemeToggle` (my change); no conflict markers. Completing the merge commit, then re-running the full gate since `main` gained m5_03's code + tests.

---
## Assistant (2026-05-31T16:49:31)

All 41 E2E pass (my 5 + m5_03's 2). Let me confirm every layer's status and that my tracker row survived the `tracker tests` step.

---
## Assistant (2026-05-31T16:49:42)

Now **all 9 layers pass** (the api flake passed this run since tenant rows existed). Gate fully green. Let me commit any tracker-tests delta, push, and merge PR #82.

---
## Assistant (2026-05-31T16:50:01)

`main` moved again — m5_03's FAQ PR merged and touched `features.md`. Re-merging and resolving the tracker row once more.

---
## Assistant (2026-05-31T16:50:11)

m5_03 is now `Merged` on main. Take its row from main, keep my m7_01 row:

---
## Assistant (2026-05-31T16:50:45)

Verify the latest merge brought in only docs (justifying no gate re-run — no code changed):

---
## Assistant (2026-05-31T16:50:58)

That diff compared against main directly (noisy). Let me check precisely what the FAQ PR added to main since my last merge:

---
## Assistant (2026-05-31T16:51:08)

The 3 commits from main are purely m5_03's FAQ (a faq markdown + tracker row→Merged) — **no code**. My code state is identical to the fully-green gate run at the prior merge (41/41 E2E, all 9 layers PASS). Gate remains valid. Merging PR #82.

---
## Assistant (2026-05-31T16:51:22)

PR #82 merged. Let me confirm `main` has my changes and the tracker row.

---
## Assistant (2026-05-31T16:51:33)

Now finalizing Phase 2: transition to `Merged`, regenerate release notes, and commit both on `main`.

---
## Assistant (2026-05-31T16:52:41)

Phase 2 done — tracker `Merged`, release notes regenerated. Now **Phase 3 — Gargi (FAQ)** for audiences school-admin, super-admin, teacher. Let me create the branch and check an existing FAQ for tone.

---
## Assistant (2026-05-31T16:52:49)

Now writing the FAQ in that voice, for school-admin / teacher / super-admin:

---
## Assistant (2026-05-31T16:53:22)

FAQ gate green (`Tracker OK`). Opening the FAQ PR and merging.

---
## Assistant (2026-05-31T16:53:44)

All three phases merged. Final verification on `main`:

---
## Assistant (2026-05-31T16:54:09)

All shipped and verified. Here's the final report.

## ✅ `/ship us-m7-01` — complete

**Feature existence check (as requested):** `US-M7-01 · Admin app shell + role-aware navigation` had **no spec dir and no tracker row** — confirmed not-yet-shipped. But the shell (AppShell + permission-keyed Sidebar + TopBar) already existed *informally* from `m1_09_role_based_ui`, which explicitly **deferred** the breadcrumb, user menu, and tenant switcher. This run formalized M7-01 and built those three pieces.

### PRs (all merged)
| Phase | PR | Status |
| --- | --- | --- |
| Spec (Vishwakarma) | **#80** | ✅ merged, branch deleted |
| Impl (Nala) | **#82** | ✅ merged, branch deleted |
| FAQ (Gargi) | **#84** | ✅ merged, branch deleted |

Tracker row now reads **Merged** (`#80`/`#82`), and `docs/RELEASE_NOTES.md` carries the M7-01 entry.

### Gates
- **Spec:** `Tracker OK` ✅
- **Impl:** all 9 layers green — ruff-lint, ruff-format, mypy, eslint, tsc, unit, **e2e 41/41**, tracker ✅. One api test (`test_m1_05_upgrade_all_applies_schema`) flaked mid-run on a DB-reseed race; **confirmed pre-existing on clean `origin/main`** and unrelated (this feature touches zero backend files), and it passed cleanly on the final full run.
- **FAQ:** `Tracker OK` ✅

### Tests — `u:0/0 a:0/0 e:5/5`
5 new Playwright specs (breadcrumb, user menu + theme, sign-out, super-admin switcher, switcher-hidden-for-tenants) + reworked the m1_09 smoke test. No unit/api: it's frontend-only and the repo has no vitest harness (same posture as m1_09).

### Decision log (front-loaded)
- **Tenant switcher → nav-only, platform console**: super-admin-only TopBar switcher over `GET /platform/tenants`; selecting lands on `/platform/tenants?tenant=<slug>` + highlights the row. **No impersonation** (entering a tenant's app needs backend token-minting → stays M8).
- **User menu → Logout (wired `useLogout`), identity (email+role), theme toggle (relocated from sidebar), Account Settings link** (placeholder `/admin/account`).
- **FAQ audience → school-admin, super-admin, teacher** (not parent).

### Notes
- Two mid-run rebases were needed: `m5_03_attendance_register` (impl PR #79/#81 then FAQ #83) landed on `main` while building, conflicting on `App.tsx`/`Sidebar.tsx`/`features.md`. Resolved cleanly — both features' changes preserved; gate re-run green after the code merge.
- Filed an implementation note flagging the `m1_05` provisioning test's DB-state flakiness as worth a separate fix.

---
## User (2026-05-31T16:57:10)

<command-message>ship</command-message>
<command-name>/ship</command-name>
<command-args>us-m7-03</command-args>

---
## User (2026-05-31T16:57:10)

Base directory for this skill: /Users/swbs/src/akshara_school_settings/akshara/.claude/skills/ship

# /ship — Ship a feature end-to-end

Run the full Akshara delivery pipeline for ONE feature with a single burst of questions up front and machine-gated auto-merge. This orchestrates the three existing skills (`vishwakarma` → `nala` → `gargi`) plus the merges and tracker transitions between them. Execute it yourself in the main thread (the sub-skills are workflows, not registered subagents).

## Operating contract (read first)

- **Front-load every decision.** Before any code, gather ALL design + audience decisions in ONE consolidated `AskUserQuestion` round (Phase 0). After that, run **unattended** — do not ask anything else unless a gate fails or you hit a hard safety block.
- **Auto-merge on green.** Merge each PR the moment its gate passes. **NEVER merge on a red gate** — stop and report instead.
- **The gates** (the only things that pause an otherwise-autonomous run):
  | Phase | Gate |
  | --- | --- |
  | Spec | `python3 scripts/tracker.py verify` → `Tracker OK` |
  | Impl | `./test/tools/run_all.sh` — every layer green. A layer may count as passing **only** if its failures are confirmed pre-existing on `main` and untouched by this feature — log that explicitly with the evidence. Otherwise stop. |
  | FAQ | `tracker verify` green |
- **Shell quoting:** write commit messages and PR bodies to temp files; use `git commit -F <file>` and `gh pr create --body-file <file>` (the bash wrapper breaks on parens/apostrophes in `-m`/`--body`). Co-author trailer on commits; 🤖 trailer on PR bodies.
- Always start each phase from a clean, synced `main` (`git checkout main && git pull --ff-only`).
- Merge with `gh pr merge <#> --merge --delete-branch`.

## Phase 0 — decisions (the one and only question burst)

1. Resolve `feat_id` + MVP story from the argument (e.g. `m1_09_rbac_matrix` / `US-M1-09`). If the tracker shows it already past `Specced`, refuse and report.
2. Read: the MVP story, `backend/rules.md`, `frontend/rules.md`, `backend/log-schema.md`, an existing spec set + FAQ for tone, and the relevant code seams.
3. Enumerate EVERY decision the feature needs: backend (tenancy: control vs tenant DB; events; RBAC keys; **external interfaces/providers**; token/cookie/storage choices; money → Decimal boundaries) · frontend (`data-testid` scope) · Gargi **audience** (parent/teacher/school-admin/super-admin).
4. Ask them ALL in a single `AskUserQuestion` round (≤4 questions; use `multiSelect` to batch sub-choices). Record the answers as a **decision log** to thread through all three phases.

## Phase 1 — Vishwakarma (specs)

Follow the vishwakarma workflow, baking in the Phase-0 decisions: write the three dense specs per the `_template` + density rules, `tracker.py init`, `spec/<feat_id>` branch + commit + push + spec PR, `tracker.py spec-pr <#>`. **Gate:** `tracker verify`. Green → merge spec PR + delete branch → `git checkout main && git pull`.

## Phase 2 — Nala (build)

Follow the nala workflow: `impl/<feat_id>` branch, `tracker.py impl-start`, implement per `design.md`, tests per `tests.md` (`test_<feat_id>_*` naming, exact IDs), self-review vs specs (≤3 passes), `./test/tools/run_all.sh` + fix (≤3 passes), write Vyasa impl notes + changelog into the spec dir, commit + push + impl PR, `tracker.py impl-pr <#>`, `tracker.py tests`, `tracker verify`. **Gate:** run_all all green (per the gate rule). Green → merge impl PR + delete branch → `git checkout main && git pull` → `tracker.py merged` → `python3 scripts/release_notes.py` (regenerates `docs/RELEASE_NOTES.md` from the merged changelogs) → commit both `docs/tracking/features.md` and `docs/RELEASE_NOTES.md` in the one `chore(tracker): … → Merged` commit and push.

## Phase 3 — Gargi (FAQ)

Follow the gargi workflow using the Phase-0 audience decision: `faq/<feat_id>` branch, write `docs/specs/<feat_id>/faq_<feat_id>.md` (plain English, dev-internals omitted, honest about not-yet-available capabilities), commit + push + FAQ PR. **Gate:** `tracker verify`. Green → merge + delete branch → `git checkout main && git pull`.

## Done — report

Summarize: the three PR numbers + merge status, each gate's result, final `u/a/e` test counts, the decision log, and anything skipped or blocked (with reason). The tracker row should read `Merged`, and `docs/RELEASE_NOTES.md` should carry the feature's entry (regenerated in Phase 2).

## The only interruptions allowed mid-run

- **A gate is red** (tests fail / tracker drift) — report it precisely; do NOT merge.
- **A hard safety block** (destroying data, reading secrets) — surface and ask.
- **A decision you failed to front-load** — ask just that one, then continue.

Anything else: proceed with the recorded decisions and sensible, spec-documented defaults.


ARGUMENTS: us-m7-03

---
## Assistant (2026-05-31T16:57:15)

Starting `/ship` for **US-M7-03**. First, Phase 0 — resolving the feature, checking it doesn't already exist, and reading the story + code seams.

---
## Assistant (2026-05-31T16:57:30)

M7-03 confirmed absent (no row, no spec dir). It's "Universal Export to Excel on list pages" — backend + frontend, with filter-respecting and streaming. Let me explore the list-endpoint and list-page seams, and note that audit already has an `audit:export` key (m1_10) which may be a template.

---
## Assistant (2026-05-31T16:57:42)

There's fresh CSV-export code in `attendance` (m5_03 register) and `students`/`staff`. Let me examine those patterns — especially the just-landed m5_03 register export and the students list filters.

---
## Assistant (2026-05-31T16:57:51)

Strong precedent: m5_03 streams CSV via `StreamingResponse` + `Iterator[str]` and emits an `*.exported` event. Now the canonical filtered list — students (m2_04):

---
## Assistant (2026-05-31T16:58:22)

The students list uses cursor pagination + filters (`q`, `status`, `section_id`, `class_id`, `academic_year_id`, `sort`). An export endpoint would reuse those filters but stream all matches. Let me check the frontend filter/toolbar seam and whether an xlsx lib is available (format decision).

---
## Assistant (2026-05-31T16:58:33)

openpyxl is available (so true .xlsx is feasible). Let me see how the StudentsPage holds its filters (state vs URL) — that determines what the export button sends.

---
## Assistant (2026-05-31T16:58:43)

Filters live in local `useState` (the export button can read the current `filter`). There's already an "Import from Excel" toolbar button as precedent. Let me check how `api.ts` maps the filter to query params (the export must reuse it) and the staff list.

---
## Assistant (2026-05-31T16:59:02)

Let me confirm whether the audit log already has its own export (m1_10) so I scope correctly, and confirm `fetchBlob`/download seam.

---
## Assistant (2026-05-31T16:59:23)

The **audit log already ships a streaming-CSV export** (`/api/v1/admin/audit/export` + `exportAuditCsv` via `fetchBlob` + anchor download) — so M7-03 generalizes that exact, proven pattern to the other list pages. Combined with m5_03's register CSV export, the house style is clearly streaming CSV. Here's my single front-loaded decision burst.

---
## Assistant (2026-05-31T17:01:12)

Decision log recorded:
- **Format** → streaming CSV (`StreamingResponse` + stdlib `csv`, opens in Excel)
- **Scope** → Students + Staff (audit keeps its own export as-is)
- **RBAC/events** → reuse `<module>:read` + emit `<module>.<noun>.exported`
- **FAQ audience** → school-admin, super-admin, teacher

Now let me study the audit export template (router+service), the students/staff service list functions, the event taxonomy, and how the audit export is e2e-tested.

---
## Assistant (2026-05-31T17:01:35)

This is the precise template: `EXPORT_CAP`, `iter_export_rows` async generator using `db.stream()` (flat memory), `_csv_line`, `StreamingResponse`. Note audit gates on `audit:read, audit:export` (two keys) — for students/staff I'll gate on `:read` only per the decision. Let me read the students/staff service list functions + filter application, and the event taxonomy.

---
## Assistant (2026-05-31T17:01:47)

The filter clauses in `list_students` (791–804) are what I'll factor into a small `_apply_student_filters` helper (mirroring audit's `_apply_filters`) so export and list share filtering. Now I need the model columns for CSV, the staff list, and the event taxonomy.

---
## Assistant (2026-05-31T17:01:54)

Let me check the StaffOut schema columns and the event taxonomy in log-schema (to name the `*.exported` events correctly).

---
## Assistant (2026-05-31T17:02:37)

Event nouns confirmed: `students.student.exported` and `staff.staff.exported` (matching the taxonomy). Let me check the Section/Class model fields so I can include human-readable labels in the student CSV.

---
## Assistant (2026-05-31T17:02:54)

I have the full picture. `Student → Section → Class` joins give human-readable class/section names. The streaming query mirrors audit's `db.stream` (flat memory), but with **no 10k cap** — the AC requires 10k+, so streaming itself is the mechanism. Starting **Phase 1 — specs**.

---
## Assistant (2026-05-31T17:04:17)

Spec gate green. Commit, push, open spec PR, backfill, merge.

---
## Assistant (2026-05-31T17:06:37)

Spec merged (PR #86). Now **Phase 2 — Nala (build)**. Syncing main and starting impl.

---
## Assistant (2026-05-31T17:06:53)

Now implementing. Let me read the exact current state of the backend files I'll modify.

---
## Assistant (2026-05-31T17:07:10)

All needed imports present except `csv` and `AsyncIterator`. Let me view the exact `list_students` body to refactor cleanly.

---
## Assistant (2026-05-31T17:07:22)

Refactoring `list_students` to share filter logic, then adding the export functions:

---
## Assistant (2026-05-31T17:07:28)

Now insert the filter helper + export functions before `list_students`, and add the `csv` / `AsyncIterator` imports:

---
## Assistant (2026-05-31T17:07:37)

Now insert the filter helper and export functions before `list_students`:

---
## Assistant (2026-05-31T17:07:57)

Now the students router — add the export endpoint. Let me read the router imports and the list endpoint area:

---
## Assistant (2026-05-31T17:08:15)

No logger in the router yet. Let me wire the new service functions into the import block, add a logger, and add the export endpoint.

---
## Assistant (2026-05-31T17:08:33)

Now add the logger import and definition, then the export endpoint after the students list endpoint. Let me add the logger:

---
## Assistant (2026-05-31T17:08:42)

Now read the current students list endpoint to add the export endpoint right after it:

---
## Assistant (2026-05-31T17:08:57)

Students backend done. Now the staff module. Let me read its service imports and router.

---
## Assistant (2026-05-31T17:09:12)

Staff service needs `csv`/`io`/`AsyncIterator`. Adding imports and export functions:

---
## Assistant (2026-05-31T17:09:31)

Staff service uses `func` — need to add it to the sqlalchemy import:

---
## Assistant (2026-05-31T17:09:35)

Now the staff router — read its service-import block and list endpoint:

---
## Assistant (2026-05-31T17:09:45)

Now add the `get_logger` and `StreamingResponse` imports to the staff router:

---
## Assistant (2026-05-31T17:09:55)

Now read the staff list endpoint to add the export endpoint after it:

---
## Assistant (2026-05-31T17:10:08)

Backend complete. Now the frontend. Let me read `lib/api.ts` and the audit download pattern to factor out:

---
## Assistant (2026-05-31T17:10:17)

Now add `triggerDownload` to `lib/api.ts` (factoring out the audit anchor logic):

---
## Assistant (2026-05-31T17:10:23)

Now the shared `ExportButton` component:

---
## Assistant (2026-05-31T17:10:35)

Now add the export functions to the feature APIs. Students first:

---
## Assistant (2026-05-31T17:10:55)

Now the staff API. Let me read it:

---
## Assistant (2026-05-31T17:11:16)

Now mount the export buttons. StudentsPage:

---
## Assistant (2026-05-31T17:11:31)

Now StaffPage — let me read it:

---
## Assistant (2026-05-31T17:11:39)

StaffPage doesn't hold the filter — let me check where StaffList keeps it:

---
## Assistant (2026-05-31T17:11:47)

Staff list shows active staff with no filter UI, so the export uses the default (active) filter — matching what's displayed. Adding the export button to StaffPage:

---
## Assistant (2026-05-31T17:12:08)

Now the tests. Let me look at existing unit + API test patterns (fixtures, how a Student is built, how the school_admin JWT is obtained).

---
## Assistant (2026-05-31T17:12:31)

I have the full test pattern. Let me check the RBAC roles to pick one lacking `students:read` for the 403 test, and confirm `staff:read` holders.

---
## Assistant (2026-05-31T17:12:51)

All defined roles include `students:read`/`staff:read`, so the 403 test uses an unknown powerless role (`["nobody"]`). Let me check `StaffCreate` fields for seeding staff in the test.

---
## Assistant (2026-05-31T17:13:00)

Now writing the API test file:

---
## Assistant (2026-05-31T17:13:47)

Now the unit tests for the pure row formatters. Students:

---
## Assistant (2026-05-31T17:14:07)

Now the E2E test. Let me check how existing specs capture a Playwright download:

---
## Assistant (2026-05-31T17:14:30)

Now the E2E spec — capturing the download and asserting the CSV header:

---
## Assistant (2026-05-31T17:15:05)

Implementation complete. Self-review against specs passes. Running the fast gate first (lint/format/mypy/eslint/tsc/unit/api) to catch issues before the slower E2E.

---
## Assistant (2026-05-31T17:15:57)

My students tests pass; the two staff export tests (a5, a6) fail, plus the known m1_05 flake. Let me see the staff failures.

---
## Assistant (2026-05-31T17:16:08)

Staff `role` must be one of `teacher, admin, support` — "accountant" is invalid. The export endpoint itself works (the teacher created fine). Fixing the test to use a valid role:

---
## Assistant (2026-05-31T17:16:20)

Let me verify my m7_03 tests now pass in isolation (api + unit):

---
## Assistant (2026-05-31T17:16:31)

All 12 m7_03 tests pass (8 api + 4 unit). The only remaining fast-run failure was the known m1_05 flake. Now the full gate including E2E.

---
## Assistant (2026-05-31T17:17:31)

43 E2E passed (41 + my 2). Let me confirm all layers and that my e2e tests ran, plus the api layer status.

---
## Assistant (2026-05-31T17:17:44)

ruff-lint and ruff-format now fail (my new backend code likely has formatting nits). Let me see and auto-fix.

---
## Assistant (2026-05-31T17:17:53)

Two long lines + format nits. Let me run ruff's autofixers (format + lint --fix):

---
## Assistant (2026-05-31T17:18:05)

Lint/format fixed (ruff reformatted 5 files, checks pass). Re-running the full gate for a clean all-green snapshot.

---
## Assistant (2026-05-31T17:19:09)

**Gate fully green — all 9 layers PASS.** Now the Vyasa impl notes + changelog, then commit/PR.

---
## Assistant (2026-05-31T17:19:44)

The impl notes claim I added the events to `log-schema.md` — I must actually do that. Let me add the two event rows.

---
## Assistant (2026-05-31T17:20:01)

log-schema updated. Now commit the implementation and open the impl PR.

---
## Assistant (2026-05-31T17:21:06)

Gate green, tracker `Implemented`. Merging the impl PR.

---
## Assistant (2026-05-31T17:21:19)

Impl PR #88 merged cleanly. Now finalizing Phase 2: `merged` transition + release notes.

---
## Assistant (2026-05-31T17:21:40)

Phase 2 done. Now **Phase 3 — Gargi (FAQ)** for school-admin / super-admin / teacher.

---
## Assistant (2026-05-31T17:22:34)

Let me do the final confirmation (spec dir complete, release notes, tracker verify):

---
## Assistant (2026-05-31T17:22:55)

All three phases shipped and verified. Final report.

## ✅ `/ship us-m7-03` — complete

**Feature:** US-M7-03 · Universal "Export to Excel" on list pages (3 pts). Confirmed absent before starting (no spec dir, no tracker row).

### PRs (all merged, branches deleted)
| Phase | PR | Gate result |
| --- | --- | --- |
| Spec (Vishwakarma) | **#86** | `Tracker OK` ✅ |
| Impl (Nala) | **#88** | **all 9 layers green** ✅ |
| FAQ (Gargi) | **#89** | `Tracker OK` ✅ |

Tracker row reads **Merged** (`#86`/`#88`); `docs/RELEASE_NOTES.md` carries the M7-03 entry.

### Final tests — `u:4/4 a:8/8 e:2/2`
- **unit**: pure `student_csv_row`/`staff_csv_row` formatters (null-handling, RFC-4180 quoting).
- **api**: content-type/header/filters (status, class), 403 (missing `students:read`), 401 (no auth), column-order.
- **e2e**: Students + Staff toolbar click → captured download → asserted CSV header.

The impl gate had **one mid-run failure** — `test_m1_05_upgrade_all_applies_schema` — the **pre-existing control-DB-state flake** (proven on clean `main` during the m7_01 ship; this branch touches no provisioning code). The final full `run_all` was **all-green**, including that test.

### Decision log (front-loaded, one burst)
- **Format** → streaming CSV (`StreamingResponse` + `db.stream` + stdlib `csv`), opens in Excel — matches the audit (m1_10) and register (m5_03) precedents and the "10k+ streaming" AC.
- **Scope** → Students + Staff (audit already exports; not refactored this round).
- **RBAC/events** → reuse each list's `:read` key + emit `students.student.exported` / `staff.staff.exported` (no new keys).
- **FAQ audience** → school-admin, super-admin, teacher.

### Notable implementation choices (documented in `design.md`/impl notes)
- **No row cap** (unlike audit's `EXPORT_CAP=10_000`) — the AC requires 10k+; `db.stream` keeps memory flat. Logged `count` for ops.
- Shared filter logic via extracted `_apply_student_filters` (list + export, one source of WHERE clauses).
- Reusable frontend `<ExportButton>` + `triggerDownload` helper; each feature's existing filter→query mapping reused.

### Deliberately deferred (flagged for follow-ups)
- CSV-injection neutralisation (matches existing exports — cross-export hardening pass).
- Multi-valued columns (guardian contact, staff subjects).
- Export on other list pages; native `.xlsx`.
- The flaky `test_m1_05_upgrade_all_applies_schema` still deserves a dedicated self-seeding fix.

No conflicts this run (clean merges throughout).

---
## User (2026-05-31T17:43:38)

<command-message>ship</command-message>
<command-name>/ship</command-name>
<command-args>us-m7-04</command-args>

---
## User (2026-05-31T17:43:38)

Base directory for this skill: /Users/swbs/src/akshara_school_settings/akshara/.claude/skills/ship

# /ship — Ship a feature end-to-end

Run the full Akshara delivery pipeline for ONE feature with a single burst of questions up front and machine-gated auto-merge. This orchestrates the three existing skills (`vishwakarma` → `nala` → `gargi`) plus the merges and tracker transitions between them. Execute it yourself in the main thread (the sub-skills are workflows, not registered subagents).

## Operating contract (read first)

- **Front-load every decision.** Before any code, gather ALL design + audience decisions in ONE consolidated `AskUserQuestion` round (Phase 0). After that, run **unattended** — do not ask anything else unless a gate fails or you hit a hard safety block.
- **Auto-merge on green.** Merge each PR the moment its gate passes. **NEVER merge on a red gate** — stop and report instead.
- **The gates** (the only things that pause an otherwise-autonomous run):
  | Phase | Gate |
  | --- | --- |
  | Spec | `python3 scripts/tracker.py verify` → `Tracker OK` |
  | Impl | `./test/tools/run_all.sh` — every layer green. A layer may count as passing **only** if its failures are confirmed pre-existing on `main` and untouched by this feature — log that explicitly with the evidence. Otherwise stop. |
  | FAQ | `tracker verify` green |
- **Shell quoting:** write commit messages and PR bodies to temp files; use `git commit -F <file>` and `gh pr create --body-file <file>` (the bash wrapper breaks on parens/apostrophes in `-m`/`--body`). Co-author trailer on commits; 🤖 trailer on PR bodies.
- Always start each phase from a clean, synced `main` (`git checkout main && git pull --ff-only`).
- Merge with `gh pr merge <#> --merge --delete-branch`.

## Phase 0 — decisions (the one and only question burst)

1. Resolve `feat_id` + MVP story from the argument (e.g. `m1_09_rbac_matrix` / `US-M1-09`). If the tracker shows it already past `Specced`, refuse and report.
2. Read: the MVP story, `backend/rules.md`, `frontend/rules.md`, `backend/log-schema.md`, an existing spec set + FAQ for tone, and the relevant code seams.
3. Enumerate EVERY decision the feature needs: backend (tenancy: control vs tenant DB; events; RBAC keys; **external interfaces/providers**; token/cookie/storage choices; money → Decimal boundaries) · frontend (`data-testid` scope) · Gargi **audience** (parent/teacher/school-admin/super-admin).
4. Ask them ALL in a single `AskUserQuestion` round (≤4 questions; use `multiSelect` to batch sub-choices). Record the answers as a **decision log** to thread through all three phases.

## Phase 1 — Vishwakarma (specs)

Follow the vishwakarma workflow, baking in the Phase-0 decisions: write the three dense specs per the `_template` + density rules, `tracker.py init`, `spec/<feat_id>` branch + commit + push + spec PR, `tracker.py spec-pr <#>`. **Gate:** `tracker verify`. Green → merge spec PR + delete branch → `git checkout main && git pull`.

## Phase 2 — Nala (build)

Follow the nala workflow: `impl/<feat_id>` branch, `tracker.py impl-start`, implement per `design.md`, tests per `tests.md` (`test_<feat_id>_*` naming, exact IDs), self-review vs specs (≤3 passes), `./test/tools/run_all.sh` + fix (≤3 passes), write Vyasa impl notes + changelog into the spec dir, commit + push + impl PR, `tracker.py impl-pr <#>`, `tracker.py tests`, `tracker verify`. **Gate:** run_all all green (per the gate rule). Green → merge impl PR + delete branch → `git checkout main && git pull` → `tracker.py merged` → `python3 scripts/release_notes.py` (regenerates `docs/RELEASE_NOTES.md` from the merged changelogs) → commit both `docs/tracking/features.md` and `docs/RELEASE_NOTES.md` in the one `chore(tracker): … → Merged` commit and push.

## Phase 3 — Gargi (FAQ)

Follow the gargi workflow using the Phase-0 audience decision: `faq/<feat_id>` branch, write `docs/specs/<feat_id>/faq_<feat_id>.md` (plain English, dev-internals omitted, honest about not-yet-available capabilities), commit + push + FAQ PR. **Gate:** `tracker verify`. Green → merge + delete branch → `git checkout main && git pull`.

## Done — report

Summarize: the three PR numbers + merge status, each gate's result, final `u/a/e` test counts, the decision log, and anything skipped or blocked (with reason). The tracker row should read `Merged`, and `docs/RELEASE_NOTES.md` should carry the feature's entry (regenerated in Phase 2).

## The only interruptions allowed mid-run

- **A gate is red** (tests fail / tracker drift) — report it precisely; do NOT merge.
- **A hard safety block** (destroying data, reading secrets) — surface and ask.
- **A decision you failed to front-load** — ask just that one, then continue.

Anything else: proceed with the recorded decisions and sensible, spec-documented defaults.


ARGUMENTS: us-m7-04

---
## Assistant (2026-05-31T17:43:42)

Starting `/ship` for **US-M7-04**. Phase 0 — resolving the feature, confirming it doesn't exist, and reading the story + code seams.

---
## Assistant (2026-05-31T17:44:15)

M7-04 confirmed absent. This is a 5-pt **read-only dashboard** aggregating four metrics from four domains (attendance %, today's fee collection, overdue count, circulars ack %). The critical question is which of those domains actually have data/endpoints built. Let me survey the modules.

---
## Assistant (2026-05-31T17:44:30)

Key signal: **attendance** is fully built, but **fees** has models but apparently **no routes**, and **circulars** is models-only (no router/service). That directly affects which dashboard metrics can use real data. Let me check fees and circulars internals.

---
## Assistant (2026-05-31T17:44:55)

Critical scoping reality emerging: **attendance** is fully built, but **fees** has tables but a *placeholder router with no routes* (no fee data flows yet), and **circulars** appears models-only. Let me confirm circulars and read the fees Invoice/Payment/Receipt columns to know what's actually queryable.

---
## Assistant (2026-05-31T17:45:34)

Now the picture is clear: **attendance** is fully live; **fees** has tables but no endpoints/data yet; **circulars** has tables (`Circular`, `CircularAcknowledgement`) but no send feature — and notably `read_at` is non-null (ack rows only exist when read), so a true ack *percentage* has no recipient denominator in the current schema. Let me read the fees Invoice/Payment columns to know what's queryable for "today's collection" and "overdue".

---
## Assistant (2026-05-31T17:46:08)

The picture is now precise. Only **attendance** is a fully live domain. **Fees** tables exist and are queryable (`payments.paid_on`/`status`, `invoices.due_date`/`status`) but no feature populates them yet → real queries return ₹0 / 0. **Circulars** tables exist but there's no send feature, and the schema has no recipient denominator, so a true ack *percentage* isn't computable yet. This is the key thing to front-load. Here's my single decision burst.

---
## Assistant (2026-05-31T17:48:42)

Decision log recorded:
- **Degradation** → all 4 cards, real queries (attendance live; fees/overdue read empty tables → ₹0/0; circular card = latest sent circular's read count, "—" when none); auto-lights-up later.
- **RBAC** → reuse `reports:read` (no new key).
- **FAQ audience** → school-admin, teacher, super-admin.

Let me check the AttendanceRecord status values (for the % query) and how tests can seed tenant-DB rows directly (to prove the fee/circular queries aren't vacuous).

---
## Assistant (2026-05-31T17:49:02)

No test seeds tenant rows directly, and tenant sessions are async (TestClient is sync). Let me check `db.py` for how a sync tenant-DB URL is built (so an api test can seed fee/circular rows to prove those queries aren't vacuous).

---
## Assistant (2026-05-31T17:49:12)

There's a sync tenant-DSN builder in `tenancy/service.py`. Let me get its exact name so the api test can seed fee/circular rows directly.

---
## Assistant (2026-05-31T17:49:56)

`make_tenant_dsn(slug)` is public — the api test can seed fee/circular rows directly. I have everything. Let me check `app/main.py` router mounting and the current Dashboard placeholder's testids (the smoke test depends on them).

---
## Assistant (2026-05-31T17:50:02)

I have the complete picture. Starting **Phase 1 — specs**.

---
## Assistant (2026-05-31T17:51:21)

Spec gate green. Commit, push, PR, backfill, merge.

---
## Assistant (2026-05-31T17:52:09)

Spec merge hit a transient "base branch was modified" (main moved). Retrying the merge.

---
## Assistant (2026-05-31T17:52:20)

Spec PR #91 merged. **Phase 2 — Nala (build).** Creating impl branch.

---
## Assistant (2026-05-31T17:52:34)

Now building the backend `dashboard` module. Let me read the AttendanceRecord columns and a minimal module (communication) as a structural template.

---
## Assistant (2026-05-31T17:53:10)

Now writing the backend `dashboard` module.

---
## Assistant (2026-05-31T17:53:55)

Now mount the dashboard router in `main.py`:

---
## Assistant (2026-05-31T17:54:31)

Backend done. Now the frontend dashboard feature.

---
## Assistant (2026-05-31T17:55:00)

Now wire `DashboardPage` into `App.tsx`, removing the placeholder. Let me read the current App.tsx:

---
## Assistant (2026-05-31T17:55:09)

I'll replace the placeholder with `DashboardPage` and remove the now-dead hello-world code.

---
## Assistant (2026-05-31T17:55:23)

Now remove the dead `SignedInBadge` and `Dashboard` functions:

---
## Assistant (2026-05-31T17:55:40)

Now rework the `@smoke` test (the `/` page changed: `dashboard-heading`/`backend-status` are gone). Let me read its current state:

---
## Assistant (2026-05-31T17:56:23)

Now rework the api test. Let me write `test/api/test_m7_04_admin_dashboard.py` — it seeds attendance/fees/circular rows directly via a sync tenant session (using `make_tenant_dsn`), since no write feature exists for fees/circulars.

---
## Assistant (2026-05-31T17:57:10)

Let me verify how `tracker.py verify` computes expected per-layer counts, to avoid a drift failure from my test count.

---
## Assistant (2026-05-31T17:57:30)

Now run the fast gate (backend lint/format/mypy + unit/api, frontend eslint/tsc) before E2E.

---
## Assistant (2026-05-31T17:58:14)

All fast layers green (224 api tests pass, including the m1_05 flake this run). I still need to write the E2E spec before the full gate.

---
## Assistant (2026-05-31T17:59:32)

Two other specs (m1_08 OTP login, m1_09 role-routing) still assert the removed `dashboard-heading`. Since the landing page legitimately changed, I'll update those references to `dashboard-page`. Let me find all of them.

---
## Assistant (2026-05-31T17:59:43)

`SignedInBadge` (testid `auth-me`) was removed too — the OTP test likely asserts it. Let me check both failing specs.

---
## Assistant (2026-05-31T17:59:53)

Both confirmed: `login-otp` asserts `auth-me` (removed with `SignedInBadge`), and `role-routing` asserts `dashboard-heading`. Both should now assert `dashboard-page` (landing on the app = signed in). Let me read and fix both.

---
## Assistant (2026-05-31T18:00:27)

Two cross-spec reworks done (landing page changed `/`). Re-running the full gate.

---
## Assistant (2026-05-31T18:01:23)

**Gate fully green — all 9 layers PASS** (unit 169, api 224, e2e 46/46). Now the Vyasa impl notes + changelog, then commit/PR.

---
## Assistant (2026-05-31T18:01:59)

Now commit the implementation and open the impl PR.

---
## Assistant (2026-05-31T18:02:54)

Gate green, tracker `Implemented`. Merging the impl PR (the diff touches `App.tsx` + shared specs, so a conflict with concurrent merges is possible).

---
## Assistant (2026-05-31T18:03:05)

Impl PR #92 merged cleanly. Finalizing Phase 2: `merged` transition + release notes.

---
## Assistant (2026-05-31T18:03:19)

Phase 2 done. **Phase 3 — Gargi (FAQ)** for school-admin / teacher / super-admin.

---
## Assistant (2026-05-31T18:04:13)

Let me do the final confirmation (full spec dir, release notes entry, clean verify):

---
## Assistant (2026-05-31T18:04:35)

All three phases shipped and verified. Final report.

## ✅ `/ship us-m7-04` — complete

**Feature:** US-M7-04 · Admin dashboard: today's stats + shortcuts (5 pts). Confirmed absent before starting.

### PRs (all merged, branches deleted)
| Phase | PR | Gate result |
| --- | --- | --- |
| Spec (Vishwakarma) | **#91** | `Tracker OK` ✅ (one transient "base modified" retry) |
| Impl (Nala) | **#92** | **all 9 layers green** ✅ |
| FAQ (Gargi) | **#93** | `Tracker OK` ✅ |

Tracker row reads **Merged** (`#91`/`#92`); `docs/RELEASE_NOTES.md` carries the M7-04 entry; all six spec files present.

### Final tests — `u:3/3 a:7/7 e:1/1`
- **unit**: `attendance_pct` boundaries (None on 0, rounding).
- **api**: empty-tenant zeros/nulls, attendance % (66.7), 403 (no `reports:read`), 401, fees+circular seeded (collection/overdue/read-count), status-filter exclusions, **Decimal boundary** (`0.10+0.20 == "0.30"`). Fee/circular rows seeded directly via `make_tenant_dsn` since no write feature exists for them.
- **e2e**: four cards render with values + overdue card drills through to `/admin/fees`.
- All 9 `run_all` layers green (unit 169, api 224, e2e 46/46); the m1_05 provisioning flake passed every run this session.

### Decision log (front-loaded, one burst)
- **Degradation → all 4 cards, real queries.** Only attendance is a live domain; fees/circulars tables read empty (₹0.00 / 0 / —) and light up automatically as those features land. The api tests prove the queries aren't vacuous by seeding rows.
- **RBAC → reuse `reports:read`** (held by school_admin + teacher; no new key, no seeding).
- **FAQ audience → school-admin, teacher, super-admin.**

### Notable choices (in design/impl notes)
- A true circular **ack %** is deferred — the schema has no recipient denominator (ack rows only exist when read), so the card shows the **read count**. Documented for the future circulars send feature.
- Money (`collection_today`) is summed as `Decimal` and serialized as a **string**; only `cleared` payments count.
- 60s auto-refresh via TanStack `refetchInterval`.

### Cross-feature reworks (the `/` landing page changed)
Replacing the hello-world placeholder removed `dashboard-heading` / `backend-status` / `auth-me`, so **three existing specs** were updated to assert `dashboard-page`: `smoke/hello` (both tests; the proxy-reachability check now uses a dashboard card load), `auth/login-otp`, and `auth/role-routing` (m1_09 e4). The dead `Dashboard`/`SignedInBadge`/`/api/hello` UI code was removed.

### Deferred (flagged for follow-ups)
- Circular ack **percentage** (needs the circulars send feature / recipient count).
- Per-class attendance breakdown, date pickers.
- `/api/hello` endpoint remains backend-side with no UI caller (harmless).

No code merge conflicts this run (only the one transient spec-PR base-moved retry).

