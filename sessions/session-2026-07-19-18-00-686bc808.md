---
type: session
project: /Users/swbs/src/akshara
branch: main
session_id: 686bc808-23a1-4c38-b816-db54eef600db
---

## User (2026-07-19T18:00:03)

I want to create a guided steps for new admin of the school to set up whole software. Example first creae academic year, then add staff, add subject etc.... it acts as guideing light and onboarding document.

---
## Assistant (2026-07-19T18:00:22)

I'll build this from what's actually merged in the repo, so the guide reflects real screens and the true dependency order. Let me first map the shipped features.

---
## Assistant (2026-07-19T18:00:39)

The nav config gives me the real screen names. Let me check where Subjects, Holidays, and a few tabbed pages live, since they're not top-level nav items.

---
## Assistant (2026-07-19T18:10:06)

The guide is written: `docs/guides/admin-onboarding.md` — a start-to-finish setup path for a new school admin, built entirely from what's actually merged (all 74 features) rather than from memory.

How it's put together:

- **A 16-step "path at a glance" table** up front, each row naming the real sidebar screen (from `nav-config.ts`) and the one-line reason it sits at that position.
- **Seven phases** in dependency order: sign in → School Settings → academic year → classes/sections → subjects → staff → class teachers → holidays → students → fee heads → structures → concessions → invoices → payments → communication → daily handover → parent rollout, plus a year-end section (promotion, status transitions, structure cloning).
- The ordering encodes the real constraints I confirmed in the FAQs: exactly one active year drives every other screen; billing flows through section assignment, so a student without a section never appears in invoice generation; Mark Attendance opens to the class teacher's section, so class-teacher assignment is its own step after staff exist; concessions are recorded before invoicing so generation subtracts them; School Settings comes first because receipts print its branding.
- Each step cites the feature's detailed FAQ by id (`m3_05` etc.), so it can serve as the spine that links into the in-app Guide later.
- It ends with a "finish line" checklist including verification steps (test payment receipt shows correct branding, test circular reaches a parent login).

Tone matches the existing `faq_*.md` guides — second person, screen names bolded, no jargon.

The file is uncommitted, on your current `impl/m2_10_subject_manager` branch's working tree. Natural follow-ups if you want them: open a docs PR for it, or go a step further and spec an in-app guided setup checklist (a wizard that tracks these steps against real data) via Vishwakarma — this document would be its content source.

