---
type: session
project: /Users/swbs/src/akshara
branch: main
session_id: 3300feef-e609-459e-8794-fbcc6b0c98ae
---

## User (2026-07-21T11:30:45)

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---
## User (2026-07-21T11:30:45)

<command-name>/model</command-name>
            <command-message>model</command-message>
            <command-args></command-args>

---
## User (2026-07-21T11:30:45)

<local-command-stdout>Set model to [1mOpus 4.8 (1M context) (default)[22m and saved as your default for new sessions</local-command-stdout>

---
## User (2026-07-21T11:31:03)

<command-message>ship</command-message>
<command-name>/ship</command-name>
<command-args>class-section-edit-plan.md</command-args>

---
## User (2026-07-21T11:31:03)

Base directory for this skill: /Users/swbs/src/akshara/.claude/skills/ship

# /ship — Ship a feature end-to-end

Run the full Akshara delivery pipeline for ONE feature with a single burst of questions up front and machine-gated auto-merge. This orchestrates the three existing skills (`vishwakarma` → `nala` → `gargi`) plus the merges and tracker transitions between them. Execute it yourself in the main thread (the sub-skills are workflows, not registered subagents).

## Operating contract (read first)

- **Front-load every decision.** Before any code, gather ALL design + audience decisions in ONE consolidated `AskUserQuestion` round (Phase 0). After that, run **unattended** — do not ask anything else unless a gate fails or you hit a hard safety block.
- **Auto-merge on green.** Merge each PR the moment its gate passes. **NEVER merge on a red gate** — stop and report instead.
- **The gates** (the only things that pause an otherwise-autonomous run):
  | Phase | Gate |
  | --- | --- |
  | Spec | `python3 scripts/tracker.py verify` → `Tracker OK` |
  | Impl | `./test/tools/run_all.sh` — every layer green. A layer may count as passing **only** if its failures are confirmed pre-existing on `main` and untouched by this feature — log that explicitly with the evidence. Otherwise stop. |
  | FAQ | `tracker verify` green |
- **Shell quoting:** write commit messages and PR bodies to temp files; use `git commit -F <file>` and `gh pr create --body-file <file>` (the bash wrapper breaks on parens/apostrophes in `-m`/`--body`). Co-author trailer on commits; plain "Generated with [Claude Code](https://claude.com/claude-code)" trailer on PR bodies — no emoji anywhere (commits, PR bodies, docs).
- Always start each phase from a clean, synced `main` (`git checkout main && git pull --ff-only`).
- Merge with `gh pr merge <#> --merge --delete-branch`.

## Phase 0 — decisions (the one and only question burst)

1. Resolve `feat_id` + MVP story from the argument (e.g. `m1_09_rbac_matrix` / `US-M1-09`). If the tracker shows it already past `Specced`, refuse and report.
2. Read: the MVP story, `backend/rules.md`, `frontend/rules.md`, `backend/log-schema.md`, an existing spec set + FAQ for tone, and the relevant code seams.
3. Enumerate EVERY decision the feature needs: backend (tenancy: control vs tenant DB; events; RBAC keys; **external interfaces/providers**; token/cookie/storage choices; money → Decimal boundaries) · frontend (`data-testid` scope) · Gargi **audience** (parent/teacher/school-admin/super-admin).
4. Ask them ALL in a single `AskUserQuestion` round (≤4 questions; use `multiSelect` to batch sub-choices). Record the answers as a **decision log** to thread through all three phases.

## Phase 1 — Vishwakarma (specs)

Follow the vishwakarma workflow, baking in the Phase-0 decisions: write the three dense specs per the `_template` + density rules, `tracker.py init`, `spec/<feat_id>` branch + commit + push + spec PR, `tracker.py spec-pr <#>`. **Gate:** `tracker verify`. Green → merge spec PR + delete branch → `git checkout main && git pull`.

## Phase 2 — Nala (build)

Follow the nala workflow: `impl/<feat_id>` branch, `tracker.py impl-start`, implement per `design.md`, tests per `tests.md` (`test_<feat_id>_*` naming, exact IDs), self-review vs specs (≤3 passes), `./test/tools/run_all.sh` + fix (≤3 passes), write Vyasa impl notes + changelog into the spec dir, commit + push + impl PR, `tracker.py impl-pr <#>`, `tracker.py tests`, `tracker verify`. **Gate:** run_all all green (per the gate rule). Green → merge impl PR + delete branch → `git checkout main && git pull` → `tracker.py merged` → `python3 scripts/release_notes.py` (regenerates `docs/RELEASE_NOTES.md` from the merged changelogs) → commit both `docs/tracking/features.md` and `docs/RELEASE_NOTES.md` in the one `chore(tracker): … → Merged` commit and push.

## Phase 3 — Gargi (FAQ)

Follow the gargi workflow using the Phase-0 audience decision: `faq/<feat_id>` branch, write `docs/specs/<feat_id>/faq_<feat_id>.md` (plain English, dev-internals omitted, honest about not-yet-available capabilities), commit + push + FAQ PR. **Gate:** `tracker verify`. Green → merge + delete branch → `git checkout main && git pull`.

## Done — report

Summarize: the three PR numbers + merge status, each gate's result, final `u/a/e` test counts, the decision log, and anything skipped or blocked (with reason). The tracker row should read `Merged`, and `docs/RELEASE_NOTES.md` should carry the feature's entry (regenerated in Phase 2).

## The only interruptions allowed mid-run

- **A gate is red** (tests fail / tracker drift) — report it precisely; do NOT merge.
- **A hard safety block** (destroying data, reading secrets) — surface and ask.
- **A decision you failed to front-load** — ask just that one, then continue.

Anything else: proceed with the recorded decisions and sensible, spec-documented defaults.


ARGUMENTS: class-section-edit-plan.md

---
## Assistant (2026-07-21T11:31:04)

I'll start by reading the plan document you've pointed me at.

---
## Assistant (2026-07-21T11:31:22)

No `m2_11` row exists, and the repo convention for enhancements to an existing story is a new slug under the same plan ID (`m2_10_staff_tabs`, `m7_02_settings_tabs`). So this should be `m2_02_class_section_edit` under **M2-02**. Let me read the code seams before asking decisions.

---
## Assistant (2026-07-21T11:31:55)

Code seams are clear. One thing the plan missed: `update_section` treats `None` as "field not provided", so there is currently **no way to unassign** a class teacher — that needs a deliberate decision. Asking everything at once:

---
## Assistant (2026-07-21T11:33:44)

Decisions recorded. Now Phase 1 — Vishwakarma specs.

---
## Assistant (2026-07-21T11:36:28)

Spec gate green. Committing and opening the spec PR.

---
## Assistant (2026-07-21T11:38:21)

Spec PR #265 merged. Phase 2 — Nala build.

---
## Assistant (2026-07-21T11:38:36)

Now the backend change (additive `q` filter):

---
## Assistant (2026-07-21T11:38:46)

Now the frontend. Staff search plumbing first:

---
## Assistant (2026-07-21T11:38:57)

Now the classes types and hooks:

---
## Assistant (2026-07-21T11:39:14)

Now the `TeacherPicker`. Let me check the `ChildSwitcher` pattern I'm copying:

---
## Assistant (2026-07-21T11:39:23)

Cross-feature `../../<feature>/api` imports are the established norm here. Writing `TeacherPicker`:

---
## Assistant (2026-07-21T11:39:48)

Now `ClassEditPanel` — the edit body with per-section save:

---
## Assistant (2026-07-21T11:40:24)

Now `ClassRow` (kebab + mode) and the read-only `SectionList`:

---
## Assistant (2026-07-21T11:40:44)

Now rewire `ClassList` to use `ClassRow` and build the teacher-name map:

---
## Assistant (2026-07-21T11:40:58)

Now the tests. API layer first:

---
## Assistant (2026-07-21T11:41:42)

Now the E2E spec, plus the rework of `class-section.spec.ts` e2:

---
## Assistant (2026-07-21T11:42:02)

Now rework `class-section.spec.ts` e2 (section creation moved into the edit panel):

---
## Assistant (2026-07-21T11:42:15)

Clean. Now running the gates:

---
## Assistant (2026-07-21T11:43:18)

My test data was wrong, not the code — "Bashir" contains "shi", not "sha". Fixing the fixture names:

---
## Assistant (2026-07-21T11:43:35)

API layer 6/6. Updating `tests.md` to match the corrected fixture names:

---
## Assistant (2026-07-21T11:43:44)

Now the full gate. Checking prerequisites first (per known-flake notes: no stale dev servers, backend needs `DYLD_FALLBACK_LIBRARY_PATH`):

---
## Assistant (2026-07-21T11:44:08)

The frontend dev server lacks `VITE_DEV_AUTH=1` — the known cause of all-logins-timing-out. Killing both so Playwright spawns correctly-configured ones with a fresh seed:

---
## Assistant (2026-07-21T11:48:46)

Root cause found — and it's the shared-tenant hazard, not my code. The snapshot after `page.reload()` shows the year select re-defaulting to a year that no longer contains the test's class: concurrent specs create and activate academic years in the same demo tenant. Dropping the reload and asserting against the post-mutation refetch, which is still a real server round-trip:

---
## Assistant (2026-07-21T11:49:05)

Now the full gate again to confirm nothing else regressed:

---
## Assistant (2026-07-21T11:53:18)

All four new tests pass; the failure is `m3_08` receipt-download — a known environment-dependent flake, and it passed in the previous gate run with this same diff. Verifying rather than assuming:

---
## Assistant (2026-07-21T11:53:41)

Confirmed flake: `m3_08` passes 3/3 on clean main, and passed in gate1 with this exact diff. My diff touches no receipt code. Running the full gate once more for an all-green run:

---
## Assistant (2026-07-21T11:57:32)

All nine layers green, 138 E2E passing. Now Vyasa docs, then the impl PR:

---
## Assistant (2026-07-21T11:59:20)

Vyasa caught a leftover — `tests.md` a5 still referenced the old fixture name. Fixing:

---
## Assistant (2026-07-21T12:01:28)

Gate is green, so merging per the auto-merge contract:

---
## Assistant (2026-07-21T12:02:16)

Phase 3 — Gargi FAQ, written for school admins and teachers:

---
## Assistant (2026-07-21T12:02:48)

The sibling FAQ now actively misleads users (it tells them to type a staff id, and promises a picker that just shipped). Correcting those three spots:

---
## Assistant (2026-07-21T12:03:56)

Shipped end to end. All three PRs merged, tracker reads `Merged`.

## PRs

| Phase | PR | Status |
| --- | --- | --- |
| Spec | [#265](https://github.com/reachsriharsha/akshara/pull/265) | merged |
| Impl | [#266](https://github.com/reachsriharsha/akshara/pull/266) | merged |
| FAQ | [#267](https://github.com/reachsriharsha/akshara/pull/267) | merged |

## Gates

- **Spec / FAQ:** `tracker verify` → `Tracker OK`
- **Impl:** `./test/tools/run_all.sh` — all nine layers PASS, 138 E2E passing. New tests: a1–a6 (API), e1–e4 (E2E). Tracker row reads `u:3/3 a:16/16 e:6/6` (the per-plan aggregate for M2-02, matching how the `m2_10_*` rows work).

## Decisions you made

- **`m2_02_class_section_edit`**, not the plan doc's `m2_11` — there is no M2-11 story, so that row's Plan column would point at nothing.
- **No teacher unassign** — assign/reassign only; documented as out of scope in `feature.md` and surfaced honestly in the FAQ.
- **Read-only view-mode chips**, all mutation in the edit panel; retires the raw staff-id input.
- **FAQ for school admin + teacher.**

## Two things worth flagging

**I amended a merged spec.** `tests.md` a1/a2 asserted that `Bashir Khan` matches `q=sha` — it doesn't (`Bashir` contains `shi`). The expectation was unsatisfiable as written, so I renamed the fixture to `Shabana Ali` and corrected the spec rows inside the impl PR. Flagging because it touches a spec frozen at merge; an amendment PR would have been the more conservative route.

**One red gate run, investigated rather than waved through.** `m3_08` receipt-download failed on the second of three full runs. Evidence it's the documented pre-existing flake and not a regression: it passed on the runs immediately before and after with the identical diff, passes 3/3 in isolation against a stashed (clean-main) tree, and this diff touches no receipt or fees code. The merge went out on a fully green third run, not on that reasoning alone.

I also killed your running dev servers before the gate — the `:5173` one lacked `VITE_DEV_AUTH=1`, which makes every E2E login time out. Playwright respawned both with the correct env and a fresh seed; you'll want to restart them if you were mid-session.

One deviation from `design.md` worth knowing about: the teacher-name query lives in `ClassList` rather than `ClassRow`, so one memoized `id -> name` Map is built once and passed down instead of per-row. Full list is in `impl_m2_02_class_section_edit.md`.

