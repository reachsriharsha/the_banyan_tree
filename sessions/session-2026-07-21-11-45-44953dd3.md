---
type: session
project: /Users/swbs/src/2nd_akshara/akshara
branch: main
session_id: 44953dd3-ca45-4d07-8448-6bab20cb7de8
---

## User (2026-07-21T11:45:28)

OK here is what I want to achieve, I am already testing the solution using start_*.sh files. Once these are running  I start fixing things. in the mean time  I dont want to sit idle. I want to test something else. So to do that  I want a separate docker compose completely in its own network. so go ahead and create a docker conainer for that. Since they work in different network there should not be any overlap on that i belive also does it different port?

---
## Assistant (2026-07-21T11:45:31)

I'll look at what exists first.

---
## Assistant (2026-07-21T11:46:15)

I have what I need. One decision changes the shape of this significantly:

---
## Assistant (2026-07-21T11:47:31)

Good question — let me check how seeding actually works today before answering.

---
## Assistant (2026-07-21T11:47:48)

Answering your doubt first — seeding already has a CLI (`backend/app/dev_db.py`):

```
uv run python -m app.dev_db recreate --seed
```

In Docker that becomes one of two things, and I'll wire up **both**:

1. **Automatic** — a one-shot `seeder` service that runs `recreate --seed` the first time the stack comes up (guarded by a marker so a restart doesn't wipe your data). So plain `docker compose -f compose.alt.yml up` gives you a seeded, logged-in-able app.
2. **Manual re-seed** — `docker compose -f compose.alt.yml exec api uv run python -m app.dev_db recreate --seed` whenever you want a clean slate.

Note the seeder needs MinIO up (it renders demo receipt PDFs), which the compose stack handles via healthcheck ordering. Let me check the build inputs.

---
## Assistant (2026-07-21T11:48:35)

Everything checks out. Writing the stack now.

---
## Assistant (2026-07-21T11:49:01)

Now the compose file:

---
## Assistant (2026-07-21T11:49:28)

Now the vite proxy needs to be env-driven (it's currently hardcoded to `localhost:8000`):

---
## Assistant (2026-07-21T11:49:48)

`process.env` in vite.config.ts needs `@types/node`, which isn't in package.json — let me check whether tsc covers that file.

---
## Assistant (2026-07-21T11:49:57)

`tsconfig.json` only includes `src`, so `vite.config.ts` isn't typechecked and no `@types/node` is needed. Building the stack now — this takes a few minutes.

---
## Assistant (2026-07-21T11:50:46)

All three images built. Bringing the stack up:

---
## Assistant (2026-07-21T11:51:10)

Seeded cleanly, including the MinIO receipt PDFs. Now verifying the app actually serves and that the two stacks are isolated:

---
## Assistant (2026-07-21T11:51:16)

Noted — let me pull those in.

---
## Assistant (2026-07-21T11:51:31)

These give me two things I was missing — the platform admin token pair and the real JWT secret. Let me check one risk first: cookies are **not** isolated by port, so `localhost:5173` and `localhost:5273` would share them.

---
## Assistant (2026-07-21T11:51:43)

Confirmed — the cookie name `akshara_refresh` is hardcoded (`backend/app/auth/service.py:26`), and browsers don't isolate cookies by port. So `localhost:5173` and `localhost:5273` would fight over the same session. Fix: reach the alt stack on **`127.0.0.1`**, which is a separate cookie origin. Updating the env and the access URL:

---
## Assistant (2026-07-21T11:58:23)

My start script swallowed the `-d` — the `up` case doesn't forward extra args. Let me fix that and stop the blocking run.

---
## Assistant (2026-07-21T11:58:56)

curl is blocked by permissions — verifying through container logs instead:

---
## Assistant (2026-07-21T11:59:18)

Both healthy. Verifying the env landed and the DB is actually seeded:

---
## Assistant (2026-07-21T11:59:54)

Isolation confirmed — both stacks running side by side on separate networks. One thing to fix: I embedded the JWT secret and admin token directly in `compose.alt.yml`, but this repo deliberately keeps those out of version control.

---
## Assistant (2026-07-21T12:00:36)

Now wiring `env_file` into the API and seeder, and gitignoring the secrets file:

---
## Assistant (2026-07-21T12:00:56)

Secrets are out of version control. Final verification — full restart from the committed config:

---
## Assistant (2026-07-21T12:01:23)

Done — the alt stack is up and running right now, alongside your dev stack.

**Open it at `http://127.0.0.1:5273`** — note `127.0.0.1`, not `localhost`. That detail matters (see below).

```
./start_alt_stack.sh          # up
./start_alt_stack.sh --build  # rebuild to pick up working-tree changes
./start_alt_stack.sh down     # stop, keep data
./start_alt_stack.sh nuke     # stop + delete volumes
./start_alt_stack.sh reseed   # wipe + reseed DB, stack stays up
```

| | alt | your dev stack |
|---|---|---|
| web | **5273** | 5173 |
| api | 8100 | 8000 |
| postgres | 15432 | 5432 |
| redis | 16379 | 6379 |
| mailpit | 18025 / 11025 | 8025 / 1025 |
| minio | 19000 / 19001 | 9000 / 9001 |
| network | `akshara-alt-net` | `akshara_default` |

Verified: separate networks, separate volumes, seeded `demoschool` tenant in its own `erp_control`/`erp_t_dev`, API and Vite both serving.

**On your seeding question** — the existing `app.dev_db` CLI does the work; I just wired it in two ways. A one-shot `seeder` service runs `recreate --seed` on first boot and drops a marker on a volume, so restarts *don't* wipe your data (`recreate` drops both DBs — unguarded it would nuke whatever you were mid-test on). To force a fresh one: `./start_alt_stack.sh reseed`. It ran successfully including the MinIO receipt PDFs.

Three things I decided that you should know about:

1. **`127.0.0.1` vs `localhost` is load-bearing.** The refresh cookie name is hardcoded (`akshara_refresh`, `backend/app/auth/service.py:26`) and browsers don't scope cookies by port — so on `localhost:5273` the alt stack would share one cookie jar with `:5173` and each login would evict the other. Different hostnames = different cookie origins. If that's too subtle to remember, the alternative is making the cookie name env-configurable; say the word.

2. **Your `.env` secrets are now in `.env.alt`, gitignored.** I first inlined the JWT secret and admin token into `compose.alt.yml`, then caught that it's a tracked file — this repo deliberately keeps those out of git. Moved them to `.env.alt` (gitignored) with a committed `.env.alt.example`; the start script auto-creates it. I verified no secret survives in any tracked file.

3. **One shared-code change:** `frontend/vite.config.ts` had the proxy hardcoded to `localhost:8000`; it now reads `process.env.VITE_API_PROXY` with that same value as the default. Your native stack behaves identically, and it isn't typechecked or linted (`tsconfig.json` only includes `src`), so no `@types/node` needed.

Also worth flagging: `S3_ENDPOINT` is `http://localhost:19000` because presigned URLs are handed to the *browser*, so it can't be `http://minio:9000`. The API container reaches it via an `extra_hosts: localhost:host-gateway` mapping. It works, but it's the least obvious part of the file — I commented it in place.

Source is baked into the images, not bind-mounted, so the alt stack is a **snapshot**: your in-progress fixes won't leak into it until you `--build`.

