---
type: session
project: /Users/swbs/src/akshara_staff_feature/akshara
branch: main
session_id: 7da1dd26-d0a5-4bb3-ab64-b47c3e385020
---

## User (2026-05-30T13:42:28)

<command-message>ship</command-message>
<command-name>/ship</command-name>
<command-args>US-M2-10</command-args>

---
## User (2026-05-30T13:42:28)

Base directory for this skill: /Users/swbs/src/akshara_staff_feature/akshara/.claude/skills/ship

# /ship — Ship a feature end-to-end

Run the full Akshara delivery pipeline for ONE feature with a single burst of questions up front and machine-gated auto-merge. This orchestrates the three existing skills (`vishwakarma` → `nala` → `gargi`) plus the merges and tracker transitions between them. Execute it yourself in the main thread (the sub-skills are workflows, not registered subagents).

## Operating contract (read first)

- **Front-load every decision.** Before any code, gather ALL design + audience decisions in ONE consolidated `AskUserQuestion` round (Phase 0). After that, run **unattended** — do not ask anything else unless a gate fails or you hit a hard safety block.
- **Auto-merge on green.** Merge each PR the moment its gate passes. **NEVER merge on a red gate** — stop and report instead.
- **The gates** (the only things that pause an otherwise-autonomous run):
  | Phase | Gate |
  | --- | --- |
  | Spec | `python3 scripts/tracker.py verify` → `Tracker OK` |
  | Impl | `./test/tools/run_all.sh` — every layer green. A layer may count as passing **only** if its failures are confirmed pre-existing on `main` and untouched by this feature — log that explicitly with the evidence. Otherwise stop. |
  | FAQ | `tracker verify` green |
- **Shell quoting:** write commit messages and PR bodies to temp files; use `git commit -F <file>` and `gh pr create --body-file <file>` (the bash wrapper breaks on parens/apostrophes in `-m`/`--body`). Co-author trailer on commits; 🤖 trailer on PR bodies.
- Always start each phase from a clean, synced `main` (`git checkout main && git pull --ff-only`).
- Merge with `gh pr merge <#> --merge --delete-branch`.

## Phase 0 — decisions (the one and only question burst)

1. Resolve `feat_id` + MVP story from the argument (e.g. `m1_09_rbac_matrix` / `US-M1-09`). If the tracker shows it already past `Specced`, refuse and report.
2. Read: the MVP story, `backend/rules.md`, `frontend/rules.md`, `backend/log-schema.md`, an existing spec set + FAQ for tone, and the relevant code seams.
3. Enumerate EVERY decision the feature needs: backend (tenancy: control vs tenant DB; events; RBAC keys; **external interfaces/providers**; token/cookie/storage choices; money → Decimal boundaries) · frontend (`data-testid` scope) · Gargi **audience** (parent/teacher/school-admin/super-admin).
4. Ask them ALL in a single `AskUserQuestion` round (≤4 questions; use `multiSelect` to batch sub-choices). Record the answers as a **decision log** to thread through all three phases.

## Phase 1 — Vishwakarma (specs)

Follow the vishwakarma workflow, baking in the Phase-0 decisions: write the three dense specs per the `_template` + density rules, `tracker.py init`, `spec/<feat_id>` branch + commit + push + spec PR, `tracker.py spec-pr <#>`. **Gate:** `tracker verify`. Green → merge spec PR + delete branch → `git checkout main && git pull`.

## Phase 2 — Nala (build)

Follow the nala workflow: `impl/<feat_id>` branch, `tracker.py impl-start`, implement per `design.md`, tests per `tests.md` (`test_<feat_id>_*` naming, exact IDs), self-review vs specs (≤3 passes), `./test/tools/run_all.sh` + fix (≤3 passes), write Vyasa impl notes + changelog into the spec dir, commit + push + impl PR, `tracker.py impl-pr <#>`, `tracker.py tests`, `tracker verify`. **Gate:** run_all all green (per the gate rule). Green → merge impl PR + delete branch → `git checkout main && git pull` → `tracker.py merged` → `python3 scripts/release_notes.py` (regenerates `docs/RELEASE_NOTES.md` from the merged changelogs) → commit both `docs/tracking/features.md` and `docs/RELEASE_NOTES.md` in the one `chore(tracker): … → Merged` commit and push.

## Phase 3 — Gargi (FAQ)

Follow the gargi workflow using the Phase-0 audience decision: `faq/<feat_id>` branch, write `docs/specs/<feat_id>/faq_<feat_id>.md` (plain English, dev-internals omitted, honest about not-yet-available capabilities), commit + push + FAQ PR. **Gate:** `tracker verify`. Green → merge + delete branch → `git checkout main && git pull`.

## Done — report

Summarize: the three PR numbers + merge status, each gate's result, final `u/a/e` test counts, the decision log, and anything skipped or blocked (with reason). The tracker row should read `Merged`, and `docs/RELEASE_NOTES.md` should carry the feature's entry (regenerated in Phase 2).

## The only interruptions allowed mid-run

- **A gate is red** (tests fail / tracker drift) — report it precisely; do NOT merge.
- **A hard safety block** (destroying data, reading secrets) — surface and ask.
- **A decision you failed to front-load** — ask just that one, then continue.

Anything else: proceed with the recorded decisions and sensible, spec-documented defaults.


ARGUMENTS: US-M2-10

---
## Assistant (2026-05-30T13:42:33)

I'll start by resolving the feature from `US-M2-10` and reading the context I need for Phase 0.

---
## Assistant (2026-05-30T13:43:05)

Feature resolves to **US-M2-10 · Staff CRUD + bulk import + class/subject mapping** (8 pts). No tracker row exists yet, so it's clear to ship. Let me read the MVP story and context seams.

---
## Assistant (2026-05-30T13:43:09)

There's already a `staff` module. Let me inspect it and use the `students` module as the reference pattern.

---
## Assistant (2026-05-30T13:44:40)

Staff module is seeded with models (Subject, Staff, StaffSubject) but no schemas/service and a placeholder router. Note the MVP AC wants `class_teacher_of` and `subjects[]` which aren't fully modeled yet. Let me study the `students` module as the reference pattern and the auto-provisioning seam.

---
## Assistant (2026-05-30T13:47:35)

Now let me study the spec tone reference, the auto-provisioning seam (users_directory), RBAC keys, migrations, and the test/frontend structure.

---
## Assistant (2026-05-30T13:54:29)

Now the key new seam for m2_10: `users_directory` auto-provisioning. Let me find how that works, plus RBAC keys, frontend structure, and tests layout.

---
## Assistant (2026-05-30T13:54:36)

`staff:*` and `students:import` keys already exist. Let me read the catalog, the UserDirectory model, and the onboarding provisioning pattern (the model for auto-creating users_directory rows).

---
## Assistant (2026-05-30T13:54:47)

Now I understand the provisioning pattern. The challenge: staff CRUD runs in an async tenant-DB request but must write `users_directory` rows in the **control DB**. Let me see how control-DB access works mid-request (idempotency ledger is control-DB).

---
## Assistant (2026-05-30T13:55:25)

Now let me check the log-schema for reserved staff events, the audit module interface, frontend structure, main.py mounting, and the test layout.

---
## Assistant (2026-05-30T13:58:36)

Now the frontend structure and test layout. Let me look at the students frontend feature, routing/sidebar, and the test harness/seeding.

---
## Assistant (2026-05-30T13:59:07)

Sidebar + route already wired. Now the test harness — API conftest/seeding, the students API test, unit test, and E2E reference.

---
## Assistant (2026-05-30T13:59:26)

Let me read the students unit test, an E2E spec + fixtures, and confirm the test runner layers.

---
## Assistant (2026-05-30T14:00:12)

I now have deep context. The `staff` module is seeded with models (`Staff`, `Subject`, `StaffSubject`) + placeholder router/page, RBAC keys (`staff:*`, `students:import`) already exist, `users_directory` lives in the control DB, and the `students` module (m2_03) is the exact pattern to mirror (router seam, `@audit`, idempotency, frontend, tests). The one genuinely new seam is cross-DB login provisioning into `users_directory`.

Let me front-load every decision in one round.

---
## Assistant (2026-05-30T14:05:17)

Decisions recorded. **Decision log:**
1. **Bulk import** — CSV upload + `GET …/import/template`; `POST …/import?dry_run=` multipart, dry-run preview then commit.
2. **Provisioning** — staff-first (tenant) then `users_directory` (control); teacher→`teacher`, admin→`school_admin`, support/no-email→no login; duplicate platform email → 409, nothing created.
3. **Mapping** — `GET/POST /admin/subjects` + `subject_ids[]` on staff→`StaffSubject`; `class_teacher_of` derived read-only from sections.
4. **FAQ audience** — school-admin, super-admin, teacher.

Let me set up tracking and check the last seams (spec template, demo seed for E2E).

---
## Assistant (2026-05-30T14:07:50)

Demo seed already has subjects (Math/Science/English), an admin + a teacher with subjects, and sections with a class teacher — E2E can build on it. I have everything. Let me set up task tracking and begin Phase 1.

---
## Assistant (2026-05-30T14:08:10)

Now Phase 1. Let me create the spec branch, init the tracker row, and write the three specs.

---
## Assistant (2026-05-30T14:11:38)

Tracker row created. Now the three specs.

---
## Assistant (2026-05-30T14:13:02)

Design is large. Let me verify one RBAC detail before writing it: teachers currently lack `staff:read`, but the FAQ audience includes teachers (read-only directory). Let me check the m1_09 matrix test blast radius.

---
## Assistant (2026-05-30T14:13:31)

Decision: grant teachers `staff:read` (one-line catalog change + matching m1_09 test update) so the directory is read-only-visible to them — matches the story and the FAQ audience; `PERMISSIONS` count stays 35 so only the teacher-set assertion changes. Now the design spec.

---
## Assistant (2026-05-30T14:14:55)

Now `tests.md`.

---
## Assistant (2026-05-30T14:15:20)

Specs written. Now run the gate (`tracker verify`), commit, push, and open the spec PR.

---
## Assistant (2026-05-30T14:16:05)

Gate green (`Tracker OK`). Commit, push, open spec PR.

---
## Assistant (2026-05-30T14:16:44)

Spec PR is #51. Backfill the tracker, merge, and sync main.

---
## Assistant (2026-05-30T14:17:08)

Main moved ahead (parallel-wave PRs landed — e.g. `m5_01_holiday_calendar`), so PR #51 has a conflict, almost certainly in `docs/tracking/features.md`. Let me resolve it.

---
## Assistant (2026-05-30T14:17:15)

The conflict is in `features.md` — main added `m2_04` and `m5_01` rows; my branch added `m2_10`. Let me merge main into the spec branch and resolve by keeping all rows.

---
## Assistant (2026-05-30T14:19:34)

Phase 1 done — PR #51 merged, main synced, `Tracker OK`. Now Phase 2 (build). Let me gather the last implementation seams before coding.

---
## Assistant (2026-05-30T14:20:10)

`fetchBlob` exists for CSV downloads — perfect for the template. Let me read StudentList + StudentForm to mirror, then write the implementation.

---
## Assistant (2026-05-30T14:20:15)

I have all patterns. Let me create the impl branch and start building.

---
## Assistant (2026-05-30T14:20:23)

Now the backend. First, schemas:

---
## Assistant (2026-05-30T14:21:36)

Now the service — the core logic including the cross-DB provisioning seam:

---
## Assistant (2026-05-30T14:22:52)

Now the router. The import endpoint uses multipart upload — let me confirm `python-multipart` is available before committing to `UploadFile`.

---
## Assistant (2026-05-30T15:13:19)

FastAPI isn't on the base interpreter — there's a venv. Let me locate it.

---
## Assistant (2026-05-30T15:14:14)

`fastapi[standard]` bundles `python-multipart` (imports fine under `uv`). Now the router:

---
## Assistant (2026-05-30T15:15:14)

Now wire `subjects_router` into `main.py`, grant teacher `staff:read`, update the m1_09 test, and flip `staff.*` live in the log schema.

---
## Assistant (2026-05-30T15:16:03)

Now update `log-schema.md` — add `staff.*` events and remove from reserved.

---
## Assistant (2026-05-30T15:16:26)

Backend wired. Now the backend unit test (u1–u3):

---
## Assistant (2026-05-30T15:17:27)

Let me quickly validate the backend unit tests + the m1_09 change compile and pass before writing the bigger API test.

---
## Assistant (2026-05-30T15:17:38)

Unit + RBAC green. Let me verify the app imports cleanly and lint the new backend files.

---
## Assistant (2026-05-30T15:17:48)

Lint flags the loop-closure (`_err` captures loop vars). Let me see all errors and fix the import loop.

---
## Assistant (2026-05-30T15:18:05)

Let me refactor the import loop to bind loop vars (fixes B023), then auto-fix imports/format.

---
## Assistant (2026-05-30T15:18:37)

Backend is clean and imports fine. Now the API integration test (a1–a14):

---
## Assistant (2026-05-30T15:19:34)

Now run the API test to validate the backend end-to-end. Let me check the api layer invocation first.

---
## Assistant (2026-05-30T15:50:33)

API tests need real Postgres. Let me run the staff API test (this requires the dev DB to be up).

---
## Assistant (2026-05-30T15:51:00)

Two issues: (1) `staff:import` isn't actually in the catalog (only `students:import` exists) — my design assumed wrong; (2) subject creation 500s. Let me see the subject error.

---
## Assistant (2026-05-30T15:51:19)

Two clear bugs: `name` is a reserved `LogRecord` key (the academic events used `class_name`/`section_name` to avoid this), and `staff:import` isn't in the catalog. Let me fix both.

---
## Assistant (2026-05-30T15:51:33)

`staff:import` is a new catalog key (not pre-existing as the design assumed — I'll note this deviation in the impl notes). Rerun the API tests:

---
## Assistant (2026-05-30T15:52:25)

Only a12 remains: the `UploadFile` param makes FastAPI parse the form *before* the idempotency guard reads `request.body()`, consuming the stream. Fix: drop the `File` param and parse the form inside the handler (after the guard has cached the body).

---
## Assistant (2026-05-30T15:52:52)

Another reserved `LogRecord` key — `created`. Rename to `created_count`.

---
## Assistant (2026-05-30T15:53:09)

All 17 staff tests pass. Now mypy --strict on the staff module (the gate runs `mypy --strict app/`):

---
## Assistant (2026-05-30T15:53:30)

Backend is green and type-clean. Now the frontend. Types first:

---
## Assistant (2026-05-30T15:53:56)

Now the components — StaffPage (replacing the placeholder), StaffList, StaffForm, StaffImportDialog:

---
## Assistant (2026-05-30T15:55:10)

Now lint + typecheck the frontend (the gate runs `eslint` + `tsc`):

---
## Assistant (2026-05-30T15:56:27)

Frontend deps aren't installed. Let me install and run the project's own lint/typecheck scripts.

---
## Assistant (2026-05-30T15:57:29)

Frontend clean. Now the E2E spec (e1, e2) — using `role=support` to avoid login provisioning for run stability:

---
## Assistant (2026-05-30T15:57:52)

Everything's written. Let me self-review against specs by running the full gate (fast first to catch lint/type/unit/api/tracker, then E2E). Running `run_all.sh --fast`:

---
## Assistant (2026-05-30T15:58:34)

One more hardcoded `35` in the m1_09 **API** test. Let me update it to 36.

---
## Assistant (2026-05-30T15:59:30)

All fast layers green (116 API passed, Tracker OK). Let me confirm every layer's status, then run the E2E layer.

---
## Assistant (2026-05-30T15:59:47)

All green except E2E (skipped). Let me check how E2E starts its servers, then run it.

---
## Assistant (2026-05-30T16:00:00)

E2E is self-contained (spawns frontend + backend with a fresh seeded DB). Let me run just my new staff spec first to validate it quickly.

---
## Assistant (2026-05-30T16:02:21)

e1 passed; e2's create hit a `Date.now()` employee_id collision (both tests run fully-parallel). Let me make the employee_id unique with a random suffix.

---
## Assistant (2026-05-30T16:02:42)

Both staff E2E pass. Now the full official gate — `run_all.sh` with all layers including the complete E2E suite:

---
## Assistant (2026-05-30T16:03:24)

Full gate green — all 9 layers PASS (16 E2E including staff-crud). Let me confirm the tracker recorded the staff test counts.

---
## Assistant (2026-05-30T16:03:38)

Tracker shows `u:3/3 a:14/14 e:2/2`. Now the Vyasa impl notes + changelog (capturing the deviations), then the impl PR. Let me write them.

---
## Assistant (2026-05-30T16:04:22)

Impl notes + changelog written. Let me review the full diff scope, then commit.

---
## Assistant (2026-05-30T16:04:34)

All expected. Let me commit the implementation, push, and open the impl PR.

---
## Assistant (2026-05-30T16:06:15)

Impl PR #56. Now the tracker transition + counts, commit, push.

---
## Assistant (2026-05-30T16:06:56)

The tracker commit didn't land (features.md still modified). Let me commit and push it.

---
## Assistant (2026-05-30T16:07:15)

Gate is green. Merge the impl PR, then sync main.

---
## Assistant (2026-05-30T16:07:25)

Main moved again. Let me merge main into the impl branch and resolve conflicts.

---
## Assistant (2026-05-30T16:07:46)

Only `features.md` conflicts. Let me see what landed on main and resolve.

---
## Assistant (2026-05-30T16:08:23)

Resolved. Let me verify the auto-merged hotspot files (catalog, main, log-schema) didn't drift, then complete the merge.

---
## Assistant (2026-05-30T16:08:42)

Post-merge sanity green (26 passed; `PERMISSIONS` still 36). Push and merge the impl PR.

---
## Assistant (2026-05-30T16:14:37)

Main advanced again (parallel wave). Re-syncing.

---
## Assistant (2026-05-30T16:14:46)

Local is already up to date with origin/main — the conflict report was stale. Retrying the merge.

---
## Assistant (2026-05-30T16:14:58)

PR #56 merged. Now finalize Phase 2: sync main, mark merged, regenerate release notes.

---
## Assistant (2026-05-30T16:15:39)

Release notes carry the staff entry. Now commit both tracker + release notes in the single `chore(tracker): … → Merged` commit and push.

---
## Assistant (2026-05-30T16:15:52)

Phase 2 complete — m2_10 is **Merged**, release notes updated and pushed. Now Phase 3 (Gargi FAQ).

---
## Assistant (2026-05-30T16:16:20)

Now the FAQ gate (`tracker verify`), commit, push, open FAQ PR.

---
## Assistant (2026-05-30T16:16:45)

FAQ PR #57, gate green. Merge and sync main (FAQ adds only a new file, so no tracker conflict expected).

---
## Assistant (2026-05-30T16:17:14)

All three PRs merged; local main == origin/main with all six spec files present. Let me record one non-obvious gotcha for future feature builds, then report.

---
## Assistant (2026-05-30T17:38:26)

`US-M2-10` is shipped end-to-end. The tracker row reads **Merged**, and `docs/RELEASE_NOTES.md` carries the feature.

## Ship report — `m2_10_staff_crud` (US-M2-10)

| Phase | PR | Status | Gate |
| --- | --- | --- | --- |
| Spec (Vishwakarma) | **#51** | ✅ merged | `tracker verify` → Tracker OK |
| Impl (Nala) | **#56** | ✅ merged | `run_all.sh` — all 9 layers green |
| FAQ (Gargi) | **#57** | ✅ merged | `tracker verify` → Tracker OK |

**Final tests:** `u:3/3 a:14/14 e:2/2` — every layer green (ruff-lint, ruff-format, mypy, eslint, tsc, unit, api, e2e, tracker).

### Decision log (Phase 0)
1. **Bulk import** → CSV upload + `GET /staff/import/template`; `POST /staff/import?dry_run=` multipart, dry-run previews then commit.
2. **Login provisioning** → staff-first (tenant) then `users_directory` (control), committed before tenant commit; `teacher→["teacher"]`, `admin→["school_admin"]`, support/no-email → no login; dup platform email → 409, nothing created.
3. **Mapping** → `GET/POST /admin/subjects` + `subject_ids[]`→`StaffSubject`; `class_teacher_of` derived read-only from sections.
4. **FAQ audience** → school-admin, super-admin, teacher.

### What shipped
Staff CRUD + subjects + CSV bulk import + cross-DB login provisioning; soft-delete→inactive; `@audit` rows (mobile/email masked); `staff.*` log events live; `features/staff/` UI (list, form, import dialog). No DB migration (seeded models already had every column).

### Deviations from the spec (recorded in impl notes)
- **`staff:import` was not actually in the catalog** (design assumed it was) — added it; `PERMISSIONS` 35→36; updated both m1_09 RBAC tests. Also granted teachers `staff:read` for the read-only directory.
- **Log-extra key renames** (`subject_name`, `created_count`) to dodge reserved `LogRecord` fields `name`/`created` that 500 at runtime — saved as a project memory for future builds.
- **Import parses the multipart form in the handler** (not a `File()` param) so the idempotency guard fingerprints the body before the stream is consumed.

### Nothing blocked
The only interruptions were **merge conflicts on `docs/tracking/features.md`** during the parallel feature wave (other M2/M5/M7 PRs landing concurrently) — resolved each time by keeping all rows; no gate was ever red and no merge happened on red.

